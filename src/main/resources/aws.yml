cloud:
  aws:
    credentials:
      accessKey: ${S3_ACCESSKEY}
      secretKey: ${S3_SECRETKEY}
    s3:
      bucket: bankidz-bucket
    region:
      static: ap-northeast-2
    stack:
      auto: false
